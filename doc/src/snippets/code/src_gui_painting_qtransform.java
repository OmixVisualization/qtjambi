import org.qtjambi.qt.gui.*;
import org.qtjambi.qt.xml.*;
import org.qtjambi.qt.network.*;
import org.qtjambi.qt.sql.*;
import org.qtjambi.qt.svg.*;


public class src_gui_painting_qtransform {
    public static void main(String args[]) {
        QApplication.initialize(args);

        /*
//! [0]
        x' = m11 + m21 + dx
        y' = m22 + m12 + dy
//! [0]


//! [1]
        x' = m11 + m21 + dx
        y' = m22 + m12 + dy
        if (is not affine) {
            w' = m13 + m23 + m33
            x' /= w'
            y' /= w'
        }
//! [1]


//! [2]
        x' = m11 + m21 + dx
        y' = m22 + m12 + dy
        if (is not affine) {
            w' = m13 + m23 + m33
            x' /= w'
            y' /= w'
        }
//! [2]


//! [3]
        x' = m11 + m21 + dx
        y' = m22 + m12 + dy
//! [3]

        */

    }
}
